// Auto-generated by Mbed CE.  Edits will be erased when CMake is rerun.
{
	"version": "2.0.0",
	"tasks": [
		// Build for target TelemetryMain
		{
			"label": "Build TelemetryMain",
			"type": "shell",
			"command": "/usr/bin/cmake",
			"args": ["--build", "/home/rombutan/Documents/SoftwareProjects/fs-3/Telemetry-Main/build", "--target", "TelemetryMain"],
		},
		// Build TelemetryMain and run the GDB server
		{
			"label": "Build TelemetryMain and start GDB server",
			"dependsOn": ["Build TelemetryMain", "GDB Server"],
			"dependsOrder": "sequence",
		},
		{
			"label": "GDB Server",
			"type": "shell",
			"command": "/usr/bin/st-util",
			"args": ["--connect-under-reset", "--listen_port=23331", "--multi"],
			"isBackground": true,
			// This task is run to start the GDB server, so that the launch configuration can connect to it.
      		// Problem is, it's a GDB server, and since it never exits, VSCode
     	 	// will never start the debug session. All this is needed so VSCode just lets it run.
			"problemMatcher": [
				{
					"pattern": [
						{
							"regexp": "________________",
							"file": 1,
							"location": 2,
							"message": 3
						}
					],
					"background": {
						"activeOnStart": true,
						"beginsPattern": ".*",
						"endsPattern": ".*",
					}
				}
			],
		}
	]

}
	